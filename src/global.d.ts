interface sideLinkType {
  link: string;
  name: string;
  icon: any;
}
interface ServicesType {
  bgImg: string;
}

interface SingleProjectType extends ServicesType {
  title: string;
  content: string;
}
